# Search Console Baseline — 2026-08-31

## Authenticated account

Google Search Console was accessed through the authenticated account the authorized Google account.

## Existing property observed

The active Google Search Console property is `https://www.aquira.art/`, not `https://www.aquira1978.com/`.

## Aquira.art baseline shown by Google Search Console

| Metric | Value |
| --- | ---: |
| Web-search clicks shown in summary | 19 |
| Impression change recommendation | -68% |
| Indexed pages | 29 |
| Not-indexed pages | 206 |

This file records an observation only; no settings were changed in Google Search Console.

## Property-addition state

The authenticated account exposes an in-product `ウェブサイトを追加` (Add website) option. No property, verification method, sitemap submission, or Google setting has yet been created or changed for `aquira1978.com`.

## Verification preparation

A URL-prefix property was selected for the canonical public URL `https://www.aquira1978.com/`. This choice permits HTML-tag verification in the static site source. The URL was entered, but the property has not been created and no verification tag has been issued yet.

## Verification dialog result

Search Console opened the two-property-type dialog. The canonical URL-prefix field is available, but no verification options have been generated yet; the prior input did not bind to the dialog field and did not create a property.

## Submitted verification request

The URL-prefix request for `https://www.aquira1978.com/` was submitted. Search Console is now attempting verification; at this point it has not shown a verification tag or a success/failure result.

## Search Console connection completed

Google Search Console automatically confirmed ownership of the URL-prefix property `https://www.aquira1978.com/`. Google reported the verification method as **HTML tag** and advised preserving the tag. The property was successfully created under the authenticated Google account. The dashboard transition was deferred because the browser DOM refreshed before the next click.

## Verified property baseline

The verified property dashboard now reports 2 web-search clicks in the displayed period, 3 indexed pages, and 10 non-indexed pages. It flags one unused verification token. The token has not been removed because deleting ownership-verification credentials changes access control and was not requested.
