{
  "name": "aquira1978-origin-archive",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "validate": "node scripts/validate-site.mjs",
    "test": "npm run build && npm run validate"
  }
}
